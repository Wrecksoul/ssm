<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE xml>
<!-- status="debug"可以看到log4j2的加载过程 -->
<Configuration status="off" monitorInterval="3600">
	<Appenders>
		<!-- 日志级别分类 - 由高到低 - OFF、FATAL、ERROR、WARN、INFO、DEBUG、TRACE、ALL -->
		<!-- 控制台输出 -->
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout
				pattern="%date{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %X{log_level} [%file] [%X{log_method}] - %msg%n" />
			<Filters>
				<!-- 设置只打印info级别的日志 -->
				<ThresholdFilter level="debug" onMatch="ACCEPT"
					onMismatch="DENY" />
			</Filters>
		</Console>
	</Appenders>
	
	<!-- 日志输入控制 -->
	<Loggers>
		<Root level="DEBUG">
			<AppenderRef ref="Console" />
		</Root>
	</Loggers>
</Configuration>